import { $fetch } from 'ofetch';
import { b as createCDNFetchLoader } from './preset-icons.b90d6d31.mjs';

function createCDNLoader(cdnBase) {
  return createCDNFetchLoader($fetch, cdnBase);
}

export { createCDNLoader as c };
